[profile.default]
src = "src"
out = "out"
libs = ["node_modules", "vendor"]
test = "test"
solc_version = "0.8.28"
evm_version = "cancun"
optimizer = true
optimizer_runs = 200
via_ir = true            # required: @gluwa/asc-contracts EvmV1Decoder._decodeReceiptChunk is stack-too-deep under legacy codegen
remappings = [
  "@gluwa/asc-contracts/=node_modules/@gluwa/asc-contracts/",
  "@openzeppelin/contracts/=node_modules/@openzeppelin/contracts/",
  "forge-std/=lib/forge-std/src/",
  "worldid/=vendor/worldid/"
]
fs_permissions = [
  { access = "read-write", path = "./" },
  { access = "read-write", path = "../deployments" },
]

[fuzz]
runs = 512

[invariant]
runs = 48
depth = 64
fail_on_revert = false

# CI runs the same suites harder: `FOUNDRY_PROFILE=ci forge test`.
[profile.ci.fuzz]
runs = 2048

[profile.ci.invariant]
runs = 128
depth = 128
fail_on_revert = false

[rpc_endpoints]
cc3 = "https://rpc.cc3-testnet.creditcoin.network"
